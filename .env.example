# Optional server-side router for every saved website submission.
# Point this at a secure Zapier Catch Hook or an integration endpoint managed by MaidFlex.
MFP_SUBMISSION_WEBHOOK_URL=

# Optional bearer token sent only from the server to the destination above.
MFP_SUBMISSION_WEBHOOK_TOKEN=
